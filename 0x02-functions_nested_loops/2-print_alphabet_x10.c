#include "main.h"
/**
 * print_alphabet - Prints the in lowercase
 * Return: always 0 (Success)
 */
void print_alphabet(void)
{
	int i;
	int a;

	for (a = 1; <= 10; a++)
	{
		for (i = 97; i = 122; i++)
		{
			_putchar(i);
		}
		_putchar('\n');
	}
}
